spring:
  datasource:
    url: jdbc:mysql://localhost:3306/searchengine?useSSL=false&serverTimezone=UTC&characterEncoding=utf8
    username: root
    password: dd.159178280
    driver-class-name: com.mysql.cj.jdbc.Driver
  
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect

logging:
  level:
    com.searchengine: DEBUG

